# Этот файл используется при деплое для переопределения настроек,
# конфликтующих с окружением сервера

services:
  db:
    # Убираем проброс порта на хост для решения проблемы с занятым портом 5432
    ports: []
    # Используем только внутреннюю сеть Docker
    expose:
      - "5432" 